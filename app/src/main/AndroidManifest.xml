<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.system.orgchatadmin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".Logout" />
        <activity android:name=".home" />
        <activity android:name=".AddUser" />
        <activity
            android:name=".HomeNav"
            android:label="@string/title_activity_home_nav"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddDepartment" />
        <activity android:name=".NewCircular" />
        <activity android:name=".AddSubDepartment" />
        <activity android:name=".SubDepartmentMessageActivity"></activity>
    </application>

</manifest>